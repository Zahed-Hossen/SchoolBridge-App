import React from 'react';
import PlaceholderScreen from '../../components/PlaceholderScreen';

const SupportScreen = (props) => (
  <PlaceholderScreen
    {...props}
    title="Support"
    icon="help-buoy-outline"
    subtitle="Get help and support here."
  />
);

export default SupportScreen;
